# クロスイメージ株式会社 経理メール運用ルール v1.0

制定日: 2026-08-14 / 対象アカウント: `trade@crossimage.jp` / 所管: 経理

> **本書の位置づけ**
> 本書は「人が読んで守る規程」であると同時に「機械が実行できる仕様書」として書かれている。
> 各ルールは *判断の余地を残さない* 形（採番式・日数・完全一致文字列）で定義する。
> 曖昧さが残る箇所は §10 の例外処理に必ず落とす。ここが崩れると自動化が成立しない。

> **機密情報の取り扱い**
> 本リポジトリは公開設定のため、振込口座番号・freee事業所番号・取引先担当者の個人アドレスは
> 本書に記載しない。`{{...}}` のプレースホルダは非公開の**取引先マスタ**（§1）を参照すること。

---

## 0. なぜルールが必要か（現状の課題）

2026年6月〜8月の実際のメール運用から抽出した、自動化を阻んでいる4つの要因。

| # | 課題 | 実例 |
|---|---|---|
| 1 | 請求書番号の採番規則が取引先ごとにバラバラ | `CRS-4002-1` / `3000-0-2` / `PO-947_1` / `HZ-S-260803` / `EVO-260724` / `CRS-GirlsDesk_20260727_1` が併存。2026-06-26に採番誤り（3001→3000）が発生し訂正メールを送付 |
| 2 | 支払条件が案件ごとの都度交渉 | 「サンプルは全額前払い」「量産は前金50%」「少額なので分割せず納品後一括」が混在し、標準が不在 |
| 3 | 請求書の追跡単位が定まっていない | 取引先から「発注毎のメールを個々に追うのが難しい。件名に対象月を記載してほしい」との改善要請あり（2026-07-29） |
| 4 | 入金消込が目視・手作業 | 未入金3件・計¥5,745,861の督促で複数往復が発生。うち1件は既に相殺精算済みだった（当方の確認漏れ） |

**結論**: 自動化できていなかったのは技術的制約ではなく、**自動化の入力となる「型」が存在しなかった**ため。本書はその型を定義する。

---

## 1. 取引先マスタ

自動化の単一の真実源（Single Source of Truth）。**非公開スプレッドシート `経理_取引先マスタ` で管理**し、本書には項目定義のみを記す。

### 1.1 必須カラム

| カラム | 型 | 説明 |
|---|---|---|
| `取引先コード` | 英大文字3桁 | 採番・ラベルの基幹キー。一度採番したら変更しない |
| `正式社名` | 文字列 | 宛名に使用。「株式会社」の前後位置を含めて正確に |
| `請求先To` | メール（複数可） | 請求書送付の宛先 |
| `請求先Cc` | メール（複数可） | 相手側の固定Cc |
| `敬称区分` | 個人名 / ご担当者様 | 個人名が判明していれば個人名を優先 |
| `支払条件区分` | A / B / C | §2 の類型 |
| `支払サイト` | 文字列 | 例: 月末締め翌月末払い / 翌々月末 |
| `例外メモ` | 文字列 | §10 で確定した恒久的な例外のみ記載 |
| `ステータス` | 取引中 / 休止 | |

### 1.2 現行取引先（コード割当・2026-08時点）

| 取引先コード | 正式社名 | 支払条件区分 | 特記事項 |
|---|---|---|---|
| `WLB` | 株式会社WILBY | B（前金50/残金50） | 少額案件は先方要望により区分C（納品後一括）。発注書No.をそのまま請求書番号に使用 |
| `HRZ` | Horizon株式会社 | B | 先方の支払サイクル都合で支払期限が翌々月末になる場合あり。請求先は経理部宛 |
| `KKK` | 有限会社カイカイキキ | A（全額前払い） | サンプル制作が中心。入稿データはSlack経由 |
| `EVO` | 株式会社EVO | A | 初期費用（刃型・金型代）は全額前払い |
| `HME` | Hamee株式会社 | C（納品後一括） | 先方から月次で「振込明細のご連絡」が届く。§8.4 で消込 |
| `MTR` | matsuri technologies株式会社 | C | **件名に対象月の明記が必須**、**発注書メールへの返信で同一スレッド維持が必須**（2026-07-29 先方要請・当方合意済み） |

> 取引先を追加する際は、**先に本マスタへ登録してコードを採番してから**請求書を発行すること。マスタ未登録のまま発行された請求書は採番衝突の原因になる。

---

## 2. 支払条件の標準（3類型）

**すべての案件は必ず A / B / C のいずれかに分類する。** 分類できない案件は §10 の例外処理へ回す。

| 区分 | 対象 | 請求タイミング | 支払期限 |
|---|---|---|---|
| **A: 全額前払い** | サンプル制作費、初期費用（金型代・刃型代）、開発着手金 | 発注書受領後ただちに100% | 請求日から **14日以内** |
| **B: 前金50% + 残金50%** | 量産、開発費、金型費など税込30万円以上の案件 | 前金: 発注書受領後ただちに<br>残金: 納品後 | 各請求日の **翌月末日** |
| **C: 納品後一括** | 税込30万円未満の少額案件、継続取引先のリピート案件 | 納品後 | 請求日の **翌月末日** |

### 2.1 適用の優先順位

1. 取引先マスタの `支払条件区分` に個別指定があればそれを最優先
2. なければ上表の金額基準で自動判定（税込30万円が閾値）
3. 先方から個別要望があった場合 → §10 例外処理

### 2.2 区分Aの着手条件

区分Aは **入金確認をもって生産着手** とする。請求書送付時点では着手しない。
文面に必ず次を含めること: 「ご入金の確認後、生産に着手いたします。」

### 2.3 共通条項（全区分・文面に必須）

- 振込手数料: **お客様負担**
- 消費税: 10%（軽減税率対象外）
- 見積書の有効期限: 発行日から **30日間**（為替変動リスクのため。延長要請は §10）

---

## 3. 請求書番号の採番規則

### 3.1 書式

```
CRS-{取引先コード}-{発注書No}-{枝番}
```

| 要素 | 規則 |
|---|---|
| `CRS` | 固定接頭辞（自社識別子） |
| `取引先コード` | 英大文字3桁。取引先マスタ §1.2 から取得 |
| `発注書No` | **先方の発注書番号をそのまま転記**。ハイフン・アンダースコアは除去し英数字のみ。発注書番号が存在しない場合のみ `YYMMDD`（発注書受領日） |
| `枝番` | `0`=全額一括（区分A・C） / `1`=前金 / `2`=残金 / `3`以降=追加請求・分割3回目以降 |

### 3.2 例

| ケース | 採番 |
|---|---|
| WILBY 発注書No.4002 の前金 | `CRS-WLB-4002-1` |
| WILBY 発注書No.4002 の残金 | `CRS-WLB-4002-2` |
| カイカイキキ サンプル全額（発注書番号なし・7/26受領） | `CRS-KKK-260726-0` |
| Horizon 発注書No.HZ03 の残金 | `CRS-HRZ-HZ03-2` |

### 3.3 不変条件（自動チェック対象）

1. **一度発番した番号は、金額・内容が変わっても再利用しない。** 訂正時は §6.7 に従い同一番号で「差し替え版」を発行する（番号は変えない）
2. 枝番の合計金額 = 発注書の総額（税込）と一致すること
3. 採番は §8.1 の請求管理台帳への登録と**同時**に行う。台帳に無い番号を送信してはならない
4. 先方の発注書番号を転記するため、**発注書PDF/Excelの番号を必ず原本で確認する**（2026-06-26の採番誤りはこの確認漏れが原因）

---

## 4. 件名・スレッド・Ccの規則

### 4.1 件名フォーマット（完全固定）

```
【御請求書】{YYYY年M月}分_{案件名}_{区分}_{請求書番号} / クロスイメージ株式会社
```

- `{YYYY年M月}` = **請求月**（請求日の属する月）。先方の月次締め処理に必要（MTR社要請）
- `{区分}` = `前金` / `残金` / `全額` のいずれか
- 例: `【御請求書】2026年8月分_AIMWIZフットレスト_前金_CRS-WLB-4002-1 / クロスイメージ株式会社`

他の書類の件名:

| 書類 | 件名フォーマット |
|---|---|
| 見積書 | `【御見積書】{YYYY年M月}分_{案件名}_{見積書番号} / クロスイメージ株式会社` |
| 入金確認 | `【入金確認】{案件名}_{請求書番号} / クロスイメージ株式会社` |
| 督促 | `【ご確認のお願い】ご請求分の入金確認について / クロスイメージ株式会社` |
| 訂正・差し替え | `【差し替え】{元の件名}` |

### 4.2 スレッド運用

**原則: 発注書を受領したメールへの「返信」で請求書を送る。** 新規スレッドを立てない。

理由: 先方が発注 → 請求 → 入金を1スレッドで追える。MTR社から明示的に要請され合意済み。前金・残金も同一スレッドに積む。

例外: 複数案件をまとめた督促（§6.6）と月次まとめ請求は新規スレッドで可。

### 4.3 Cc規則

| 種別 | 宛先 |
|---|---|
| 自社側Cc（**全送信メールに常時付与**） | `info@crossimage.jp`, `koizumi@crossimage.jp`, `chen@crossimage.jp` |
| 相手側Cc | 取引先マスタの `請求先Cc` + **受信メールに付いていたCcを全員維持** |

> 返信時に相手側Ccを削ると、先方社内の情報共有が切れて督促の往復が増える。**Ccは減らさない**を原則とする。

### 4.4 署名（完全固定・改変禁止）

```
━━━━━━━━━━━━━━━━━━━━━━━
クロスイメージ株式会社
〒150-0011 東京都渋谷区東3-8-22 伊藤ビル2F
TEL: {{代表電話}}
E-mail: trade@crossimage.jp
━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 5. ファイル保存とGoogle Drive連携

> **重要 — 保存先のルールは既に存在する**
> 共有ドライブ `Crossimage` 直下の **`00_フォルダの使い方.pdf`**（最終更新 2026-06-12／整理担当: 秘書AI）に、
> 保存先と命名規則が既に明文化されている。**本条はそれを置き換えるものではなく、追認したうえで
> 「メール運用と接続する部分」を補うものである。**
> 既存ルールと本書が矛盾する場合は、§5.6 の差分表に従う。

### 5.1 既存の保存先ルール（`00_フォルダの使い方.pdf` より・そのまま有効）

```
10_クライアント: 会社別 → 年別 → 案件フォルダ（YYMMDD【案件番号】内容）。
                発注書・請求書・納品書はすべてここ。
                資料や開発物は各社の「資料・出荷関係」へ
```

| # | 既存ルール | 状態 |
|---|---|---|
| 1 | ファイル名は `YYYYMMDD_相手先や案件_書類種別.pdf` | 有効（§5.3 で実態に合わせて確定） |
| 2 | フォルダ直下に置かず、必ず **会社 → 年 → 案件フォルダ** の中へ | 有効 |
| 3 | 同じファイルを2か所に置く時は、コピーせず Drive の**ショートカット**を使う | 有効 |
| 4 | `(1)` `コピー` 付きファイルは作らない（修正版は旧版を `90_アーカイブ` へ移して差し替え） | 有効 |
| 5 | スキャナ由来の `doc12345.pdf` は、その日のうちに内容の名前へ変更 | 有効 |

### 5.2 なぜ保存されていないのか

**既存ルールは「どこに置くか」を定義しているが、「いつ・誰が・何をトリガーに置くか」を定義していない。**
そのため、メールを処理した時点で保存動作が発生せず、Drive への反映が個人の裁量に委ねられている。

実際に生じている断絶（2026-08-14 時点で確認）:

| 事象 | 詳細 |
|---|---|
| **WILBY【4002】が Drive に存在しない** | 2026-08-10 に発注書受領、2026-08-11 に前金請求書を送付済みだが、Drive 内に `4002` を含むファイル・フォルダが**1件も無い**。メールだけで完結してしまっている |
| フォルダ名が実態とずれている | `260409【3000】発注書・前金` の中に `20260630_3000_請求書_残金.pdf` が入っている。残金を追加した際にフォルダ名が更新されていない |
| 会社フォルダ直下への直置き | `WILBY/AIMdesk_変更要求_工場用_中国語版.xlsx` 等（既存ルール2違反） |
| `(2)` 付きファイルの存在 | `AIMdesk_変更要求客人回复_工場用_中国語版-0730(2).xlsx`（既存ルール4違反） |
| 会社直下の構造が2系統に分岐 | 年別（`2023`〜`2026`）と、トピック別（`昇降デスク` `不良品・品質` `AIM Chair Pro 請求・入金` 等）が混在。KAIKAIKIKI では製品別フォルダと `_未分類` も併存 |
| `30_経理` に `契約書` フォルダが重複 | 同名フォルダが2つ存在する |
| 未整理フォルダの滞留 | `_まとめ由来_要確認` / `_mac mini由来_要確認` / `_未分類` |

**本条が追加するのは §5.4 の「保存タイミング」である。** ここが欠けている限り、保存先をいくら精緻に定義しても運用されない。

### 5.3 命名規則（実態に合わせて確定）

`260409【3000】` フォルダ内の実ファイルは、以下の形式で一貫している。**これを正とする。**

```
{YYYYMMDD}_{発注書No}_{書類種別}_{区分}.{拡張子}
```

| 実例 | 内訳 |
|---|---|
| `20260409_3000_発注書.xlsx` | 区分なし（発注書は分割されない） |
| `20260409_3000_請求書_前金.pdf` | 前金 |
| `20260630_3000_請求書_残金.pdf` | 残金 |

- `{YYYYMMDD}` = その書類の発行日（受領書類は受領日ではなく**書類上の日付**）
- `{書類種別}` = `発注書` / `見積書` / `請求書` / `納品書` / `検収書`
- `{区分}` = `前金` / `残金` / `全額`。発注書・見積書には付けない
- Excel 原本と PDF の両方がある場合は**両方保存する**（実態がそうなっている。PDF が送付版、xlsx が編集原本）

> **§3 の請求書番号との関係**
> ファイル名には `CRS-WLB-` 接頭辞を含めない。会社フォルダが `WILBY` である時点で取引先は確定しており、
> 区分も末尾に書かれているため、`{発注書No}` + フォルダ位置から請求書番号 `CRS-WLB-3000-1` は一意に復元できる。
> 冗長な二重管理を避けるため、**ファイル名は実態の短い形式を維持する**。

### 5.4 保存タイミング（本条の中核・新規）

**保存はメール処理と同一の作業単位とする。「後でまとめて」は禁止。**

| # | トリガー | 動作 |
|---|---|---|
| **S1** | **発注書メールを受領した時**<br>（Gmailラベル `02_発注書/未対応` が付いた時） | ① 会社 → 年 の下に案件フォルダを作成（§5.5）<br>② 受領した発注書の原本を `{日付}_{発注書No}_発注書.{ext}` として保存<br>③ 台帳（§8.1）に行を追加し、`Driveフォルダリンク` を記入 |
| **S2** | **請求書を送付した時**<br>（§7 のチェック通過・送信直後） | 送付した請求書の PDF と Excel 原本を、S1 で作った案件フォルダへ保存 |
| **S3** | **納品書・検収書を授受した時** | 同じ案件フォルダへ保存 |
| **S4** | **訂正・差し替えが発生した時** | 旧版を `90_アーカイブ` へ移動してから、新版を同名で保存（既存ルール4に従い `(1)` を作らない） |

**S1 と S2 は、メール処理を「完了」と見なすための必須条件とする。** 案件フォルダが存在しない状態で請求書を送ってはならない。

> **台帳に `Driveフォルダリンク` 列を追加する**（§8.1 を改訂）。
> メールスレッドID・請求書番号・Driveフォルダの3点が1行に揃うことで、
> 「どのメールの、どの請求書が、どこに保存されているか」が1箇所で引ける状態になる。

### 5.5 案件フォルダの命名 — 改訂提案

現行の `YYMMDD【発注書No】{含まれる書類}` は、**書類を追加するたびにフォルダ名の変更を要求する**。
実際に `260409【3000】発注書・前金` は残金追加時にリネームされておらず、この方式は既に破綻している。

**新規案件から、末尾を「含まれる書類」ではなく「案件名」に変更する。**

```
YYMMDD【発注書No】案件名
```

| | 現行 | 改訂後 |
|---|---|---|
| 例 | `260409【3000】発注書・前金` | `260409【3000】AIMdeskPro開発費` |
| リネーム | 書類追加のたびに必要 | 不要 |
| 中身の把握 | フォルダ名を信頼できない | フォルダを開けば分かる（かつ名前が案件を示す） |

- **既存フォルダはリネームしない。** 移行コストとリンク切れのリスクに見合わない。新規案件から適用する
- 発注書番号が無い案件は `YYMMDD【-】案件名` とし、`【-】` で「番号なし」を明示する

### 5.6 本書と既存 `00_フォルダの使い方.pdf` の差分

| 項目 | 既存PDF | 本書 | 扱い |
|---|---|---|---|
| 保存先の階層 | 会社 → 年 → 案件 | 同左 | **一致**。変更なし |
| ファイル名の例示 | `20260612_CRS-1015_請求書_前金.pdf` | `20260409_3000_請求書_前金.pdf` | **実態を採用**。`CRS-` 接頭辞は付けない（§5.3） |
| 案件フォルダ名 | `YYMMDD【案件番号】内容` | `YYMMDD【発注書No】案件名` | **新規案件のみ改訂**（§5.5） |
| 保存タイミング | 規定なし | S1〜S4 を規定 | **本書で新規追加**（§5.4） |
| ショートカット活用 | 規定あり | 同左 | 一致。特に `20_物流・通関` と案件フォルダの二重保存はショートカットで解決する |

> **`30_経理/入出金（自動投入）` には手を触れないこと。**
> 既存ドキュメントに「`po_agent` の自動保存先（手で触らない）」と明記されている。
> 本書の自動化（§12）を実装する際も、このフォルダを出力先にしてはならない。

### 5.7 メール添付時の扱い

- 添付ファイル名に含まれる `{発注書No}` と `{区分}` は、メール本文の請求内容と一致していなければならない（§7 チェック項目④）
- 参考として先方の発注書を再添付する場合は、**原本のファイル名を変更せず**そのまま添付する

---

## 6. メール文面テンプレート

`{{ }}` は差し込み変数。**それ以外の文言は改変しない**（改変すると自動生成との差分検知が効かなくなる）。

### T1. 発注書受領 → 前金請求（区分B）

```
{{正式社名}}
{{担当者名}} 様

いつも大変お世話になっております。
クロスイメージ株式会社でございます。

このたびは「{{案件名}}」の発注書（No.{{発注書No}}）をお送りいただき、
誠にありがとうございます。ご発注を確かに承りました。

つきましては、前金50%分の御請求書を添付にてお送りいたします。

【請求内容】
　請求書番号　：{{請求書番号}}
　案件名　　　：{{案件名}}
　御請求日　　：{{YYYY年M月D日}}
　お支払期限　：{{YYYY年M月D日}}
　納品予定日　：{{YYYY年M月D日}}
　前金（税抜）：¥{{税抜金額}}
　消費税(10%)　：¥{{消費税額}}
　御請求金額　：¥{{税込金額}}（税込）

※上記は発注金額（税込¥{{発注総額}}）の前金50%分です。残金50%は納品後にご請求いたします。
※お振込手数料はお客様のご負担にてお願いいたします。

【振込先口座】
{{振込先ブロック}}

ご確認のほど、何卒よろしくお願いいたします。

{{署名}}
```

### T2. 残金請求（区分B）

T1 と同一構造。差分のみ:

- 本文2段落目 → `標記「{{案件名}}」につきまして、お支払い条件（前金50%・残金50%）に基づき、残金分（50%）の御請求書をお送りいたします。`
- 明細見出し → `残金（税抜）`
- 注記 → `※上記は発注金額（税込¥{{発注総額}}）の残金50%分です。前金分（{{前金請求書番号}}）は{{入金確認日}}に入金を確認しております。`

### T3. 全額前払い請求（区分A）

```
{{正式社名}}
{{担当者名}} 様

いつもお世話になっております。
クロスイメージ株式会社の張です。

{{案件名}}の発注書を拝受しました。御請求書をお送りいたします。

■ ご請求について
{{対象物}}は全額（100%）でのご請求となります。

　請求書番号　：{{請求書番号}}
　御請求金額　：¥{{税込金額}}（税込／内訳：税抜¥{{税抜金額}}＋消費税¥{{消費税額}}）
　お支払期限　：{{YYYY年M月D日}}

■ 納期について
ご入金の確認後、生産に着手いたします。
納品予定は{{納品予定}}頃を見込んでおります。

※お振込手数料はお客様のご負担にてお願いいたします。

【振込先口座】
{{振込先ブロック}}

ご査収のほど、よろしくお願い申し上げます。

{{署名}}
```

### T4. 見積書送付

```
{{正式社名}}
{{担当者名}} 様

いつも大変お世話になっております。
クロスイメージ株式会社でございます。

{{案件名}}につきまして、御見積書をお送りいたします。

【御見積内容】
　見積書番号　：{{見積書番号}}
　御見積金額　：¥{{税込金額}}（税込）
　有効期限　　：{{YYYY年M月D日}}（発行日から30日間）
　納期目安　　：{{納期}}
　お支払条件　：{{支払条件区分の説明}}

※有効期限は為替変動に備えて設けております。単価は極力据え置くよう努めますが、
　為替の状況によっては変動する可能性がございます。
※お振込手数料はお客様のご負担にてお願いいたします。

ご確認のほど、何卒よろしくお願い申し上げます。

{{署名}}
```

### T5. 入金確認のご連絡

> **送信タイミング: 入金確認の当日中。** 先方に安心感を与え、二重支払い・督促の行き違いを防ぐ。区分Aでは着手の合図も兼ねるため省略不可。

```
{{正式社名}}
{{担当者名}} 様

いつもお世話になっております。
クロスイメージ株式会社の張です。

「{{案件名}}」（請求書番号：{{請求書番号}}）につきまして、
{{YYYY年M月D日}}付でご入金を確認いたしました。
早速お手続きいただき、誠にありがとうございます。

{{区分Aの場合のみ：つきましては、本日より生産に着手いたします。納品予定は{{納品予定}}頃を見込んでおります。}}

引き続き、何卒よろしくお願いいたします。

{{署名}}
```

### T6. 督促（3段階）

**共通の必須文言**（誤督促のリスクヘッジ。2026-07-28に精算済み案件を督促してしまった事例あり）:

```
行き違いですでにお振込済みの場合は、何卒ご容赦ください。
```

#### T6-1. 一次督促（期限+3営業日）

```
{{正式社名}}
{{担当者名}} 様
（関係各位）

いつも大変お世話になっております。
クロスイメージ株式会社の張でございます。

標記の件につきまして、下記のご請求分の入金が現時点で確認できておりません。
お手数ですが、ご入金状況をご確認くださいますようお願い申し上げます。

【未確認のご請求】
　請求書番号：{{請求書番号}}　御請求日：{{請求日}}　ご請求金額：¥{{税込金額}}（税込）
　（複数ある場合は行を追加し、末尾に合計行）

【振込先口座】
{{振込先ブロック}}

行き違いですでにお振込済みの場合は、何卒ご容赦ください。
まだご入金いただけていない場合は、速やかにご入金くださいますようお願い申し上げます。

念のため、該当の請求書を添付いたします。
ご確認のほど、何卒よろしくお願いいたします。

{{署名}}
```

#### T6-2. 二次督促（期限+10営業日）
T6-1 に加えて:
- Ccに先方の**経理部門・上長**を追加
- 冒頭に `{{請求日}}および{{一次督促日}}にご連絡いたしました件につきまして、再度のご連絡となり恐縮です。` を挿入
- 末尾に `お支払い予定日について、{{期限+15営業日}}までにご回答いただけますと幸いです。` を追加

#### T6-3. 三次督促（期限+20営業日）
- **メール送信前に社内エスカレーション**（経理→代表）。メール単独で送らない
- 電話連絡を併用し、通話内容を請求管理台帳の `備考` に記録
- 文面は二次督促を踏襲し、`本件につきまして、{{期限+25営業日}}までにご入金またはお支払い予定日のご回答をいただけない場合、別途ご相談させていただく場合がございます。` を追加

### T7. 訂正・差し替え

```
{{正式社名}}
{{担当者名}} 様

いつも大変お世話になっております。
クロスイメージ株式会社でございます。

このたびは{{誤りの内容}}に誤りがございましたこと、誠に申し訳ございません。
ご指摘のとおり、正しくは「{{正しい値}}」でございます。

{{誤りの内容}}を修正のうえ、差し替え版を添付にてお送りいたします。
お手数をおかけし恐縮ですが、先にお送りした請求書（{{誤った内容}}）は破棄いただき、
本メールの添付をご査収くださいますようお願い申し上げます。

{{署名}}
```

> **請求書番号は変更しない。** 内容を訂正した場合もファイル名末尾に `_差替` を付すのみとし、番号を振り直さない（台帳の一意性を保つため）。

---

## 7. 送信前チェックリスト（6点・全項目必須）

請求書メールの送信前に、以下を**すべて**確認する。1つでも未確認なら送信しない。

- [ ] **①採番**: 請求書番号が §3 の書式に合致し、請求管理台帳に登録済み。**発注書原本の番号と一致**している
- [ ] **②金額整合**: 税抜 + 消費税 = 税込。前金/残金の場合は「枝番合計 = 発注総額」も検算
- [ ] **③支払期限**: 取引先マスタの支払条件区分（§2）どおりの日付。休日の場合は翌営業日
- [ ] **④添付整合**: 添付PDFのファイル名の請求書番号と、本文の請求書番号が完全一致。添付し忘れがない
- [ ] **⑤宛先**: 宛名・敬称が取引先マスタどおり。自社Cc3名 + 相手側Cc（受信時のCc全員を維持）
- [ ] **⑥スレッド**: 発注書受領メールへの返信になっている。件名が §4.1 の書式（**請求月の明記**を含む）

### 7.1 送信後の必須動作

- [ ] **⑦ Drive保存**: 送付した請求書の PDF と Excel 原本を案件フォルダへ保存した（§5.4 S2）。**保存をもってメール処理を完了と見なす**

> ②と④は 2026-06-26 の採番誤り、⑥は 2026-07-29 の先方要請、⑦は WILBY【4002】の Drive 未保存に対応する項目。**チェック省略が過去に実害を出している。**

---

## 8. 入金消込と督促のタイムライン

### 8.1 請求管理台帳（自動化のバックボーン）

非公開スプレッドシート `経理_請求管理台帳` に、**請求書発行と同時に1行を追加**する。

| カラム | 説明 |
|---|---|
| `請求書番号` | 主キー。§3 の書式 |
| `取引先コード` / `取引先名` | |
| `案件名` | |
| `相手発注書No` | |
| `区分` | 前金 / 残金 / 全額 |
| `請求日` | |
| `税抜` / `消費税` / `税込` | |
| `支払期限` | §2 から自動算出 |
| `入金予定日` | 先方から回答があった場合のみ（例: HRZ社の翌々月末回答） |
| `入金日` / `入金額` / `差額` | 消込時に記入。差額≠0 は要調査 |
| `ステータス` | `未送付` / `入金待ち` / `入金済` / `督促中` / `相殺・精算済` |
| `メールスレッドID` | Gmail のスレッドID。再送要請時に即座に遡れる |
| `Driveフォルダリンク` | 案件フォルダのURL（§5.4 S1 で記入）。メール・請求書・保存先が1行で揃う |
| `添付ファイル名` | |
| `備考・例外` | §10 で承認された例外のみ |

> **`相殺・精算済` ステータスは必須。** 2026-07-28 に「先方が相殺処理済みだった案件」を未入金として督促してしまった。台帳に相殺を記録する場所が無かったことが原因。

### 8.2 スケジュール（営業日ベース・自動実行）

| タイミング | アクション | 送信先 |
|---|---|---|
| D+0（請求書送付日） | 台帳に `入金待ち` で登録。Gmailラベル `01_請求/入金待ち` を付与 | — |
| 支払期限 −3営業日 | **社内リマインドのみ**（先方には送らない） | 社内 |
| 支払期限 +1営業日 | 入金照合を実行。確認できたら T5 送信・`入金済` へ | 先方 |
| 支払期限 +3営業日 | 一次督促 T6-1。`督促中` へ | 先方 |
| 支払期限 +10営業日 | 二次督促 T6-2（Ccに先方経理・上長を追加） | 先方 |
| 支払期限 +20営業日 | 社内エスカレーション → 三次督促 T6-3 + 電話 | 社内 → 先方 |

### 8.3 督促前の必須確認（誤督促の防止）

督促メールを送る前に、必ず次の3点を確認する。

1. 台帳のステータスが `相殺・精算済` でないこと
2. 当該取引先から `入金予定日` の回答が来ていないこと（回答済みならその日まで督促しない）
3. 該当スレッドに先方からの支払関連の返信が来ていないこと

### 8.4 入金照合のルール

- 照合キー: **振込名義 + 金額**
- 複数請求がまとめて1件で振り込まれる場合（HME社の「振込明細のご連絡」等）は、先方の明細メールを根拠に**手動で分割消込**し、台帳の `備考` に明細メールのスレッドIDを記録する
- 差額が発生した場合（振込手数料の先方差引など）は `差額` に記録し、¥1,000未満は経理判断で `入金済` として処理可。¥1,000以上は先方に照会する

---

## 9. Gmailラベル体系とフィルタ

### 9.1 ラベル体系

```
01_請求/送付済
01_請求/入金待ち
01_請求/入金済
01_請求/督促中
02_発注書/未対応
02_発注書/請求済
03_見積/送付済
03_見積/受注
03_見積/失注
04_支払（自社が支払う側）
05_会計/freee
05_会計/税理士
06_取引先/WLB
06_取引先/HRZ
06_取引先/KKK
06_取引先/EVO
06_取引先/HME
06_取引先/MTR
99_要対応
```

> 現在このアカウントには**ユーザー定義ラベルが1つも存在しない**（システム標準のINBOX/SENT/IMPORTANT等のみ）。ラベルが無いため、フィルタも組めず、状態管理がすべて人の記憶に依存している。ここが Phase 0 の最優先着手点。

### 9.2 Gmailフィルタ設定（そのまま登録可能）

| # | 条件 | 動作 |
|---|---|---|
| F1 | `subject:(発注書 OR ご発注 OR 発注) has:attachment -from:me` | `02_発注書/未対応` を付与 + スター + 重要マーク |
| F2 | `from:me subject:【御請求書】` | `01_請求/送付済` と `01_請求/入金待ち` を付与 |
| F3 | `subject:(振込 OR ご入金 OR 入金 OR お支払) -from:me` | `99_要対応` を付与 + 重要マーク |
| F4 | `from:(freee.co.jp)` | `05_会計/freee` を付与 |
| F5 | `from:({{税理士事務所ドメイン}})` | `05_会計/税理士` を付与 |
| F6 | `from:({{WILBY担当者アドレス}})` | `06_取引先/WLB` を付与 |
| F7〜F11 | 各取引先について F6 と同様 | `06_取引先/{{コード}}` を付与 |
| F12 | `from:(info@tailscale.com OR events@tailscale.com OR workspace-noreply@google.com OR googleaistudio-noreply@google.com)` | 受信トレイをスキップ + 既読化（サービス通知が経理メールを埋没させている） |

> F12 の対象は実際の受信内容から抽出。現在、受信トレイの相当数がSaaSのマーケティングメールで占められており、`IMPORTANT` マークも機能していない。

### 9.3 ステータス遷移の規約

ラベルは**排他**とする。遷移時は必ず旧ラベルを外す。

```
02_発注書/未対応 → 02_発注書/請求済ヽ
                                    ＞ 01_請求/入金待ち → 01_請求/入金済
                  01_請求/送付済  ノ                    ↘ 01_請求/督促中 → 01_請求/入金済
```

---

## 10. 例外処理ルール

**自動化を壊す最大の要因は、その場限りの例外を記録せずに処理してしまうこと。**

### 10.1 例外の定義

次のいずれかに該当したら例外として扱う。

- 先方から支払条件（分割・期限・支払サイト）の変更依頼があった
- 標準の3類型（§2）に当てはめられない案件
- 相殺・値引き・生産中止に伴う減額が発生した
- 請求書の再発行・差し替えが発生した

### 10.2 処理手順

1. **その場で判断せず、まず台帳の `備考・例外` に事実を記録する**（依頼日・依頼者・内容・根拠メールのスレッドID）
2. 影響額が税込30万円以上、または支払期限が30日以上後ろ倒しになる場合は、**代表の承認**を得てから先方に回答する
3. 回答後、当該案件の台帳行を更新する
4. **同一取引先で2回以上繰り返された例外は、例外ではなく仕様である。** 取引先マスタの `支払条件区分` または `例外メモ` に恒久ルールとして昇格させ、以後は標準処理とする

### 10.3 既知の例外（昇格待ち）

| 取引先 | 内容 | 発生日 | 判定 |
|---|---|---|---|
| WLB | 少額案件は分割せず納品後一括請求 | 2026-07-31 | §2 区分Cの金額基準に統合済み |
| HRZ | 支払サイクル都合により支払期限を翌々月末に変更 | 2026-08-12 | **要観察**。次回も同様なら取引先マスタの支払サイトを「翌々月末」に変更 |
| MTR | 件名に対象月を明記／発注書メールへの返信で同一スレッド維持 | 2026-07-29 | §4.1・§4.2 の全社標準に昇格済み |

---

## 11. freee連携ルール

### 11.1 現在の制約（2026-08時点・期間限定）

弥生会計からfreee会計へのデータ移行（2026-07-16納品）において、**freee社側の作業ミスによりインボイス制度の経過措置（80%控除）の税区分が誤って登録された**ことが 2026-08-12 に判明。freee社が修正作業を実施予定（2026-08-21までに日程連絡）。

**修正作業日は、当日14:00〜18:00の間、以下の操作を行ってはならない**（freee社からの指示）:

1. 移行対象期間以前の期間へのデータ登録
2. マスタの設定内容変更（勘定科目・取引先・品目）
3. 口座の新規登録・口座の名称変更・口座種別の変更
4. 固定資産台帳の登録
5. 年度締め機能などによる年度の変更

### 11.2 移行期間中の運用ルール

- **freee側の税区分修正が完了するまで、freeeへの自動連携（API連携・自動仕訳ルール）を新規に構築しない。** 不安定な土台の上に自動化を積むと、修正作業で二重に壊れる
- この期間の自動化投資は、Gmail側（§9）と請求管理台帳（§8.1）に限定する。これらはfreeeから独立しており、修正作業の影響を受けない
- 修正完了後、メモタグ `データ移行サービス_税区分修正_mmdd` で絞り込み、当該仕訳の税区分・税額を経理と税理士で照合する
- 消費税申告について、freee移行後に申告を行っていた場合は**修正申告の要否を税理士に確認する**（freee社より要確認との指摘あり）

### 11.3 完了後の連携方針

税区分修正の完了確認後、以下の順で連携する。

1. 請求管理台帳 → freee の売上仕訳を CSV インポート（手動・月次）
2. freee の入金明細 → 請求管理台帳の消込を半自動化
3. 安定を確認してから API 連携へ移行

---

## 12. 自動化ロードマップ

| Phase | 内容 | 前提条件 | 効果 |
|---|---|---|---|
| **Phase 0**<br>即日着手可 | ラベル体系の作成（§9.1）／フィルタ12本の設定（§9.2）／取引先マスタの整備（§1）／請求管理台帳の作成（§8.1）／テンプレートの下書き登録（§6）／**Drive保存タイミングS1〜S4の運用開始（§5.4）と未保存案件の遡及登録** | なし。**freeeの修正作業と一切干渉しない** | 状態が可視化され、埋没・失念が消える |
| **Phase 1**<br>freee修正完了後 | 台帳を起点とした督促の自動起票（§8.2のスケジュールをスクリプト化）／送信前チェック（§7）の自動検証 | Phase 0 完了 | 督促漏れゼロ。採番・金額ミスの機械検知 |
| **Phase 2** | 発注書メール受信 → 請求書の自動起票（採番・金額計算・PDF生成・下書き作成まで。**送信は人が承認**） | Phase 1 が3ヶ月安定稼働 | 1件あたりの作業時間を大幅短縮 |
| **Phase 3** | freee API 連携による入金消込の自動化 | §11.3 の1・2が安定 | 消込作業の実質自動化 |

### 12.1 自動送信に関する原則

**請求書・督促メールの「送信」は、当面すべて人の承認を経る。** 自動化するのは下書き生成・チェック・リマインドまでとする。

金額と信用に直結する文書であり、誤送信のコストが自動化の便益を上回る。Phase 2 でも下書き止まりとするのはこのため。

---

## 13. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v1.0 | 2026-08-14 | 初版制定。2026年6〜8月の実運用メールから現行パターンを抽出し標準化 |
