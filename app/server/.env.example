# Anthropic API（未設定ならモックAIに自動フォールバック）
ANTHROPIC_API_KEY=

# セッション署名用シークレット（本番では必ず変更）
SESSION_SECRET=change-me-in-production

# 提案を即APPROVEDにする（true）か承認キューに入れる（false）
AUTO_APPROVE_PROPOSALS=false

PORT=8787

# シード用初期パスワード（npm run seed が参照）
SEED_STAFF_PW=changeme-staff
SEED_CLIENT_PW=changeme-client
