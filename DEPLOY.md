# 公開手順（デプロイ）— オーナー作業は数クリックだけ

「デプロイ」とは、今は開発環境の中にしかないこのシステムを、インターネット上のサーバーに置いて
`https://〜` のURLでお客様・中国側・スマホから開けるようにする作業のことです。

技術的な設定はすべてこのリポジトリに入れてあります（`render.yaml` と `Dockerfile`）。
ホスティング先は **Render**（https://render.com）を使います。GitHubと連携してクリックだけで
公開でき、以後は私がコードを更新するたびに自動で反映されます。

---

## オーナーにお願いする作業（初回のみ・約5分・**カード登録不要**）

現在の設定は【デモ公開モード＝無料プラン】です。上司やお客様に実際に操作してもらえます。

1. **アカウント作成**: https://render.com を開き「Sign Up」→「GitHub」を選んでログイン
   （新しいパスワードを作る必要はありません。GitHubアカウントでそのまま入れます）
2. **公開**: 画面上部の「New +」→「**Blueprint**」を選択 →
   リポジトリ一覧から `fulla61/predict_stock` を選ぶ → ブランチに
   `claude/crossimage-product-os-y44ms4` を指定 →「Apply」を押す
   - 途中で `ANTHROPIC_API_KEY` の入力欄が出たら **空のまま** でOKです（デモAIで動きます）
3. **完了**: 5〜10分待つと `https://crossimage-product-os.onrender.com` のようなURLが表示されます。
   それがそのまま公開URLです。お客様画面=そのURL、社内管理画面=末尾に `/admin`

**無料プランの制約（デモには支障なし）**: 15分アクセスが無いとスリープし、次に開いた人の
初回表示に約1分かかります。また再起動・再デプロイのたびにデータは初期状態へ戻ります。

**本運用へ切り替える時（実案件を入れる前に）**: カード登録のうえ、私に「本運用モードにして」と
言ってください。設定ファイルを有料プラン（約$7/月）+永続ディスクへ切り替えてプッシュすれば、
自動で再デプロイされ、以後データが消えなくなります。

## ログイン情報の確認場所

初期パスワードは安全のため自動生成されます。Renderの管理画面 →
`crossimage-product-os` → 左メニュー「**Environment**」で確認できます。

| 用途 | メールアドレス | パスワード |
|---|---|---|
| 社内（管理コンソール） | `admin@crossimage.jp` | `SEED_STAFF_PW` の値 |
| デモ顧客（お客様画面の確認用） | `demo@example.co.jp` | `SEED_CLIENT_PW` の値 |

## 本物のAIに切り替える時（任意・後からでOK）

今はデモAI（定型ロジック）で動作します。本物のAIにするには:

1. https://console.anthropic.com でAPIキーを作成（従量課金。通常の利用なら月数百円〜数千円程度）
2. Render管理画面 →「Environment」→ `ANTHROPIC_API_KEY` にキーを貼り付けて保存
3. 自動で再起動し、同じ画面のままAIが本物になります（画面右上の「デモAI」表示が消えます）

## 中国側からの閲覧について

Renderの標準URL（`〜.onrender.com`）は中国から概ね閲覧できますが、通信環境により
遅い・繋がりにくい場合があります。実際に工場側で開けるか試してもらい、問題があれば
独自ドメイン＋CDN（中国向け配信の仕組み）を次の段階で追加します。
それまでの代替として、RFQ本文の `.md` ダウンロード → WeChat転送の運用が使えます。

## 以後の更新について

`render.yaml` の `autoDeploy: true` により、このブランチにコードがプッシュされるたびに
自動で新しいバージョンが公開されます。オーナーの作業は不要です。

## 技術メモ（参考）

- 構成: 単一Dockerコンテナ（Express + SQLite + Reactビルド済み静的ファイル）
- データ: Renderの永続ディスク（1GB）を `/data` にマウント。SQLiteのDBはそこに保存
- 初期データ投入は起動時に自動実行（冪等: 2回目以降は何もしない）
- セッションcookieは本番で `secure` 属性付き。TLSはRenderが自動付与
- ヘルスチェック: `/api/health`
